import { View, Text } from 'react-native';
import React from 'react';

interface Props {}

const Login = (() => {
  return (
    <View>
      <Text>Login</Text>
    </View>
  );
}) as React.FC;

export default Login;
